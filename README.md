# digitalmarketplace-status

## init

`npm install;  
npm install -g electron;
`

## run app
`electron .`

## package application
`npm install -g electron-packager;  
electron-packager . DM-STATUS --electronVersion 1.4.13;`
